import React from "react";
import "../css/ShowCase.css";
import Fade from "react-reveal/Fade";

const Interior = ({ img, title, desc, leftBtnText, rightBtnText }) => {
  return <></>;
};

export default Interior;
